# playwright

Headed Playwright MCP server — the default browser automation plugin. Opens a visible Chrome window for collaborative browsing, login flows, and interactive web tasks.

This is the **default** Playwright plugin. Use `playwright-headless` for background tasks or as a WebFetch fallback.

## Install

```
/plugin install playwright@alexv-claude
```
