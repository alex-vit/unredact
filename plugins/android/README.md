# android

Known gotchas when working with Android SDK command-line tools: emulators, `apksigner`, `sdkmanager`, AVD creation, and APK signing with key rotation.

Automatically referenced when working with Android emulators, signing APKs, or managing SDK components.

## Install

```
/plugin install android@alexv-claude
```
